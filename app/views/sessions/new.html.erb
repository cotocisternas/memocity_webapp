<% provide(:title, "Iniciar sesión" )%>
<div class="row">
	<div class="col-md-4 col-md-offset-4">
		<h1><%= yield(:title)  %></h1>
		<%= form_for(:session, url: login_path) do |f| %>
		<%= f.email_field :email, class:"form-control", placeholder: "Correo" %>
		<%= f.password_field :password, class:"form-control", placeholder: "Contraseña" %>
		<%= link_to "¿Olvidaste la contraseña?", new_password_reset_path %>
		<br>
		<%= f.label :remember_me, class: "checkbox" do %>Mantener sesión
		<%= f.check_box :remember_me %>
		<% end %>
		<p>
			<%= f.submit "Ingresar", class: "btn btn-primary" %>
		</p>
		<% end %>
		<p>¿Nuevo en Memocity? <%= link_to 'Registrar', signup_path %> </p>
	</div>
</div>
